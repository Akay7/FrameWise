services:
  - type: web
    name: framewise-demo
    runtime: docker
    dockerfilePath: ./Dockerfile.demo
    plan: free
    # No provider secrets needed: visitors supply their own API key via the
    # app's UI (see app.py). PORT is injected by Render automatically.
